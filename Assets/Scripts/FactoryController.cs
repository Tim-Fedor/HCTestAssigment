using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FactoryController : BaseFactory
{
    public FactoryController(FactoryConfig config) : base(config)
    {
    }
    
    
}
